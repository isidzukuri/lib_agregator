
<%= render partial: "layouts/book_search_form" %>

<!-- <br/>
<br/>
<br/>
<br/>
<br/>
<div class="row">
  <h3>Рекомендовані книги</h3>
</div> -->

<div class="row">
  <% @paper_books.each do |book|%>
  <a href="/books/<%= book.seo %>" class="col-md-4 col-xs-6 book_panel_link">
    <img src="<%= book.cover %>" alt="<%= book.title %>" title="<%= book.title %>" />
    <!-- <%= book.title %> -->
  </a>
  <% end %>
  
  <% @free_books.each do |book|%>
  <a href="/books/<%= book.seo %>" class="col-md-4 col-xs-6 book_panel_link">
    <img src="<%= book.cover %>" alt="<%= book.title %>" title="<%= book.title %>" />
    <!-- <%= book.title %> -->
  </a>
  <% end %>

</div>


<!-- <div class="row">
  <h2 class="section_title">Книги для скачування:</h2>
</div>  -->
